type Query
type Mutation
type Subscription

enum EventType {
  CREATED
  UPDATED
  DELETED
}

input StringFilter {
  equals: String
  contains: String
  startsWith: String
  endsWith: String
  mode: QueryMode
}

enum QueryMode {
  default
  insensitive
}